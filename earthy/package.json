{
  "name": "@earthy",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @earthy/web dev",
    "build": "pnpm --filter @earthy/web build",
    "db:generate": "pnpm --filter @earthy/db db:generate",
    "db:migrate": "dotenv -e apps/web/.env.local -- pnpm --filter @earthy/db db:migrate",
    "db:push": "dotenv -e apps/web/.env.local -- pnpm --filter @earthy/db db:push",
    "db:pull": "dotenv -e apps/web/.env.local -- pnpm --filter @earthy/db db:pull",
    "db:studio": "dotenv -e apps/web/.env.local -- pnpm --filter @earthy/db db:studio",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv-cli": "^10.0.0"
  },
  "packageManager": "pnpm@10.33.4"
}
